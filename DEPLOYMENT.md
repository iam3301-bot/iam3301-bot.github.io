# 🚀 Steam API 部署指南

本项目已经实现了**无需本地服务器**的 Steam API 调用方案，支持所有访问 GitHub Pages 的用户。

## 📊 方案概览

### 当前实现的方案

✅ **方案 1: 公共 CORS 代理**（已启用，无需配置）
- 使用 `allorigins.win` 和 `corsproxy.io` 作为备用代理
- 自动故障转移
- 无需任何配置即可使用

⚪ **方案 2: Cloudflare Workers**（可选，推荐用于生产环境）
- 更稳定、更快速
- 需要一次性配置
- 完全免费（每天 100,000 次请求）

---

## ✅ 方案 1: 使用公共 CORS 代理（默认已启用）

### 优点
- ✅ 无需配置，开箱即用
- ✅ 自动备用切换
- ✅ 适合个人项目和测试

### 缺点
- ⚠️ 可能不稳定（依赖第三方服务）
- ⚠️ 可能有频率限制
- ⚠️ 速度可能较慢

### 使用说明

**无需任何操作**，项目已经配置好公共代理，可以直接使用。

打开 `game-detail.html` 查看游戏详情时，会自动尝试获取 Steam 推荐配置。

---

## 🌟 方案 2: 部署到 Cloudflare Workers（推荐）

### 优点
- ✅ 稳定可靠
- ✅ 全球 CDN，速度快
- ✅ 完全免费（每天 100,000 次请求）
- ✅ 自定义域名支持

### 部署步骤

#### 步骤 1: 注册 Cloudflare 账号

1. 访问 [Cloudflare 注册页面](https://dash.cloudflare.com/sign-up)
2. 使用邮箱注册（免费账号即可）
3. 验证邮箱

#### 步骤 2: 创建 Worker

1. 登录 Cloudflare Dashboard
2. 点击左侧菜单 **Workers & Pages**
3. 点击 **Create application**
4. 选择 **Create Worker**
5. 给 Worker 起个名字，例如：`steam-proxy`
6. 点击 **Deploy**

#### 步骤 3: 编辑 Worker 代码

1. 部署后会看到编辑器
2. 点击右上角 **Quick edit**
3. 删除所有默认代码
4. 复制 `/home/user/webapp/worker.js` 的全部内容
5. 粘贴到编辑器中
6. 点击 **Save and deploy**

#### 步骤 4: 获取 Worker URL

部署成功后，你会看到类似这样的 URL：

```
https://steam-proxy.your-subdomain.workers.dev
```

**复制这个 URL！**

#### 步骤 5: 配置前端使用你的 Worker

编辑 `steam-api.js` 文件：

```javascript
// 找到这一行（约第 11 行）
{
  name: 'Cloudflare Worker',
  baseUrl: 'https://steam-proxy.yourdomain.workers.dev', // 👈 替换这里
  enabled: false, // 👈 改为 true
  priority: 1
},
```

修改为：

```javascript
{
  name: 'Cloudflare Worker',
  baseUrl: 'https://steam-proxy.your-subdomain.workers.dev', // 👈 替换成你的URL
  enabled: true, // 👈 改为 true
  priority: 1
},
```

#### 步骤 6: 测试

1. 保存文件
2. 提交到 GitHub
3. 访问 GitHub Pages
4. 打开游戏详情页
5. 查看浏览器控制台，应该看到：`[SteamAPI] ✅ 成功使用: Cloudflare Worker`

---

## 🧪 测试你的 Worker

部署后，可以直接访问测试：

### 测试端点

```bash
# 访问根路径，查看使用说明
https://your-worker.workers.dev/

# 测试搜索功能
https://your-worker.workers.dev/api/steam?term=Dota2

# 测试详情功能
https://your-worker.workers.dev/api/steam/appdetails?appid=570
```

---

## 📊 方案对比

| 特性 | 公共代理 | Cloudflare Worker |
|------|----------|-------------------|
| **配置难度** | ⭐ 无需配置 | ⭐⭐ 简单配置 |
| **稳定性** | ⭐⭐ 中等 | ⭐⭐⭐⭐⭐ 优秀 |
| **速度** | ⭐⭐ 中等 | ⭐⭐⭐⭐⭐ 非常快 |
| **免费额度** | 有限 | 100,000 次/天 |
| **推荐用途** | 测试、个人项目 | 生产环境 |

---

## 🔧 故障排除

### 问题 1: 所有代理都失败

**解决方案：**
1. 检查网络连接
2. 查看浏览器控制台错误信息
3. 尝试直接访问代理 URL 测试
4. 如果使用 Cloudflare Worker，确认已正确部署

### 问题 2: Cloudflare Worker 部署失败

**解决方案：**
1. 确认账号已验证邮箱
2. 检查代码是否完整复制
3. 查看 Worker 日志（Cloudflare Dashboard）
4. 尝试重新创建 Worker

### 问题 3: 找不到游戏推荐配置

这是正常的，原因可能是：
- Steam 上该游戏没有推荐配置数据
- 游戏名称不匹配
- Steam API 暂时不可用

---

## 💡 高级配置

### 添加自定义域名（可选）

1. 在 Cloudflare Worker 设置中
2. 点击 **Triggers**
3. 添加 **Custom Domain**
4. 配置你的域名

### 查看使用统计

1. Cloudflare Dashboard
2. Workers & Pages
3. 选择你的 Worker
4. 查看 **Metrics** 标签

---

## 📝 更新日志

### v2.0.0 (2024-12-02)
- ✅ 添加公共 CORS 代理支持（默认启用）
- ✅ 添加 Cloudflare Workers 支持（可选）
- ✅ 实现自动故障转移
- ✅ 优化错误处理
- ✅ 支持多端点配置

### v1.0.0 (Initial)
- 基于本地 Node.js 服务器（已弃用）

---

## 🤝 需要帮助？

- 查看 [README.md](README.md) 主文档
- 提交 [GitHub Issue](https://github.com/iam3301-bot/iam3301-bot.github.io/issues)
- 查看 [Cloudflare Workers 文档](https://developers.cloudflare.com/workers/)

---

**推荐配置：**
- 开发/测试：使用默认的公共代理
- 生产环境：部署 Cloudflare Worker（10 分钟配置，终身受益）
